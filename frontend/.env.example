# SafetyLens Frontend

# Copy to `.env.local` for local development.
# Provider credentials belong in the backend environment, never in the browser.

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_DEMO_FALLBACK=true
